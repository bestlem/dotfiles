
# hack to get working so can build dotfiles installer
import pathlib

xontrib load coreutils prompt_ret_code z powerline  whole_word_jumping
# The following seem to require xpip to be run - which needs a sudo
#xontrib load vox vox_tabcomplete avox docker_tabcomplete

new_paths = [ '~/bin', '/opt/local/bin' ]
new_paths.reverse()
for x in new_paths:
	$PATH.insert(0, x)

$LSCOLORS='gxfxbEaEBxxEhEhBaDaCaD'
$XONSH_COLOR_STYLE='fruity'

aliases['ll'] = 'ls -AGFlha'
aliases['lr'] = 'ls -AGFlhart'
aliases['l@'] = 'ls -AG@Flha'
aliases['l'] = 'ls -AG@Fha'
aliases['e'] = 'open -a Aquamacs.app'
aliases['man'] = 'openman'


def _env(args, stdin=None):
	/usr/bin/env | sort

aliases['env'] = _env
